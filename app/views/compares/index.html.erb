<script>
  var PageData = {
    league_id: <%= params[:league_id].to_i %>,
    user_id: <%= params[:user_id].to_i %>
  };
</script>
<div class="row container-fluid topfix container-space">
  <div class="trading-teams">
    <div class="col-md-6">
      <h1><%= @user_team_name %></h1>
      <%= render 'user_team' %>
    </div>

    <div class="row">
      <div class="col-md-3">
        <h1>Trading Team</h1>
      </div>

      <div class="col-md-3">
        <%= render 'team_selector' %>
      </div>

      <div class="col-md-6">
        <div class="render_trade_table" id="otherTeamTable">
        <!-- RENDER THE AJAX TABLE HERE -->
        </div>
      </div>
    </div>
  </div>
  <button class="btn btn-default btn-lg goaliebtn" id="add-to-trade"> Select Players For Trade </button>

  <container id="hidden-trades" class="hidden">
  <div class="row container-fluid topfix container-space">
    <div class="col-md-12">
    <!-- Players to be traded table -->
    <%= render partial: 'to_be_traded_tables', locals: { idPlayer: "user-output", idGoalie: "user-output-goalies", tableHeader: "Your Players"}  %>
    <%= render partial: 'to_be_traded_tables', locals: { idPlayer: "other-output", idGoalie: "other-output-goalie", tableHeader: "Opponent's Players"}  %>
    </div>
  </div>
  </container>
  <!-- TEAM STAT IMPACT -->
  <div class="row container-fluid topfix container-space">
    <div class="col-md-12">
      <%= render 'team_stat_impact' %>
    </div>
  </div>
  <div class="col-md-12 graph" id="neg-value-column_top" style="width:100%; height:400px;"></div>
</div>
